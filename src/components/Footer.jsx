function Footer() {
  return (
    <footer className="app-footer">
      Note: We are using an API, so we are not responsible if you find any
      line a bit creepy.
    </footer>
  );
}

export default Footer;
