function Footer() {
  return (
    <footer className="app-footer">
      <span className="app-footer-icon" aria-hidden="true">
        ⚠️
      </span>
      This is just for fun. Lines come from a random third-party API, so we
      are not responsible if you find one a bit creepy.
    </footer>
  );
}

export default Footer;
